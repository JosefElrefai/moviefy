import React from 'react';
import { connect } from 'react-redux';

class headerSliderContent extends React.Component {
    render(){
        return (<div>aaa</div>);
    }
} 

export default  connect()(headerSliderContent);